apiVersion: v1
kind: Service
metadata:
  name: ocr-hw-frontend
spec:
  type: LoadBalancer
  ports:
    - port: {{ .Values.frontend.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: ocr-hw-frontend
