apiVersion: v1
kind: Service
metadata:
  name: ocr-hw-notification-service
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.notificationService.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: ocr-hw-notification-service
