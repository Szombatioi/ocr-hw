apiVersion: v1
kind: Service
metadata:
  name: ocr-hw-backend
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.backend.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: ocr-hw-backend
